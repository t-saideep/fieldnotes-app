{
  "name": "fieldnotes-server",
  "version": "1.0.0",
  "description": "Backend for fieldnotes app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup-vss": "npm install && npm install @sqlite-vss/darwin-x64 && echo 'SQLite VSS extension installed'"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "openai": "^4.0.0",
    "sqlite3": "^5.1.6",
    "uuid": "^9.0.0",
    "@sqlite-vss/darwin-x64": "^0.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 